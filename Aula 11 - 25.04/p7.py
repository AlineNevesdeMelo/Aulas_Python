#Altere o programa a seguir para mostrar os número ÍMPARES entre 1 e 20

for num in range(1, 21): 
     if num % 2 != 0: 
           print (num)

#Cometário: Nesse programa foi analisado uma lista de números de 1 a 20, sob a condição
#de encontrar os números que tivessem o resto da divisão por dois diferente de zero,
#ou seja, encontrar e mostrar os números impares dentro do intervalo de 1 a 20.